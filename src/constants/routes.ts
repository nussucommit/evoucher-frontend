export enum Routes {
  index = "/",
  register = "/register",
  login = "/login",
  forgotPassword = "/forgotpassword",
  changePassword = "/changepassword",
  adminHome = "/admin",
  adminLogin = "/admin/login",
}
